from dpipe.torch.model import TorchModel

use_cuda = cuda = True

model = TorchModel(
    model_core=model_core,
    logits2pred=logits2pred,
    logits2loss=logits2loss,
    optimize=optimize,
    cuda=cuda
)
