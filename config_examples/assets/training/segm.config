from .base import *

from dpipe.medim.metrics import average_metric, compute_dices_from_segm_prob

val_metrics = {
    "dice_scores": average_metric(
        # lazy
        metric=compute_dices_from_segm_prob(
            # lazy
            segm2msegm=dataset.segm2msegm
        )
    )
}
