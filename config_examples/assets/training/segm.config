from .base import *

from dpipe.medim.metrics import average_metric, compute_segm_dices

val_metrics = {
    "dice_scores": average_metric(
        # lazy
        metric=compute_segm_dices(
            # lazy
            segm2msegm=dataset.segm2msegm
        )
    )
}
